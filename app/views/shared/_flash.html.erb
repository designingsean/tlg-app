<% if flash %>
  <% flash.each do |type, msg| %>
    <div class="alert alert-<%= flash_class(type) %> alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= msg.html_safe %>
    </div>
  <% end %>
<% end %>